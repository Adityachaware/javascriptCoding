// 10) JavaScript Program to Calculate the Area of a Triangle.


let breadth = 15;

let height = 10;

let area = (b,h) => (b * h * 0.5);  // area of triamgle formula is 0.5*b*h

console.log(area(breadth,height));