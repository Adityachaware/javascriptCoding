// 13) Javascript Program to Check if a number is Positive, Negative, or Zero

let number13 = -1;

let checkNumber = (value) => (value === 0) ? "zero" : (value > 1) ? "positive": "negative"; 

console.log(checkNumber(number13)); // here number will be negative;